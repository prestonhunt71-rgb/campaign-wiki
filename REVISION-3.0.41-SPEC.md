# Revision 3.0.41 — District map lettering

- Draw a thin dotted boundary around each district in its cropped district map.
- Place a translucent, outlined vintage-style name inside each district polygon, wrapping and sizing the lettering to fit.
- Keep the citywide map and map thumbnails unchanged.

Validation: district map tests pass. The full non-browser suite is run before release.
